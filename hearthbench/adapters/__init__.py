"""Model adapters (A2): a `ModelAdapter` Protocol one interface all
backends implement, formalizing the existing `build_llm_client`
factory in `hearthmind.llm.client` (A0.1 — reuse, don't rebuild) for
bench use. Not yet implemented — module reserved by the A1 package
skeleton.
"""
