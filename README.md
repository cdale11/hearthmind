# Hearthmind 0.2.0

## Run

```bash
cd hearthmind   # project root containing pyproject.toml
./scripts/install.sh
./scripts/run.sh
```

Open http://localhost:8000

If using another machine:
http://<server-ip>:8000
